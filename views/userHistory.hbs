<div class="container">
<h1>WEB322 {{session.user.username}} ( {{session.user.email}} ) History</h1>
<a href="/logout" class="btn btn-danger">Logout</a>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Login Date/Time</th>
      <th scope="col">Client Information</th>
    </tr>
  </thead>
  <tbody>
    {{#each session.user.loginHistory}}
    <tr>
      <td>{{dateTime}}</td>
      <td>{{userAgent}}</td>
    </tr>
    {{else}}
    <tr>
        <td colspan="2">No Data Available</td>
    </tr>
    {{/each}}
  </tbody>
</table>
</div>